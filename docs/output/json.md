# Module: json

**0 metrics.**

Back to [overview](README.md).

The JSON collector is configuration-driven: metric names, labels, Redfish paths, and JQ extraction filters are all supplied at runtime via the exporter configuration file. There are no predefined metrics — what gets emitted depends entirely on the `json_collector` stanzas in your config. See the exporter documentation for configuration syntax.

